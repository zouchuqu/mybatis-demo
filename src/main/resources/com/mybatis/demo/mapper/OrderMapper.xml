<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.demo.mapper.OrderMapper">
    <!--
    嵌套结果：使用嵌套结果映射来处理重复的联合结果的子集
                               封装联表查询的数据(去除重复的数据)
     select * from order o,user_inf u where o.user_id=u.id and o.id=#{id}
     -->
    <select id="selectOrdersAndUserInfoByOrderID" resultMap="getOrderAndUserInfo">
        select * from orders o,user_info u where o.user_id=u.id and o.id=#{id}
    </select>

    <resultMap type="com.mybatis.demo.model.Orders" id="getOrderAndUserInfo">
        <!--
            id:指定查询列表唯一标识，如果有多个唯一标识，则配置多个id
            column:数据库对应的列
            property:实体类对应的属性名
          -->
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="number" property="number"/>
        <!--association:用于映射关联查询单个对象的信息
            property:实体类对应的属性名
            javaType:实体类对应的全类名
          -->
        <association property="userInfo" javaType="com.mybatis.demo.model.UserInfo">
            <!--
                id:指定查询列表唯一标识，如果有多个唯一标识，则配置多个id
                column:数据库对应的列
                property:实体类对应的属性名
              -->
            <id column="id" property="id"/>
            <result column="username" property="username"/>
            <result column="password" property="password"/>
            <result column="high" property="high"/>
            <result column="age" property="age"/>
            <result column="sex" property="sex"/>
        </association>
    </resultMap>


    <!--
         方式二：嵌套查询：通过执行另外一个SQL映射语句来返回预期的复杂类型
         select user_id from order WHERE id=1;//得到user_id
         select * from user WHERE id=1   //1 是上一个查询得到的user_id的值
         property:别名(属性名)    column：列名 -->
    <select id="getOrdersByOrderId" parameterType="int" resultMap="getOrderMap">
        select * from orders where id=#{id}
    </select>

    <resultMap type="com.mybatis.demo.model.Orders" id="getOrderMap">
        <id column="id" property="id"/>
        <id column="user_id" property="userId"/>
        <result column="number" property="number"/>

        <!--property="userInfo" : orders 中要查询的属性-->
        <!--column="user_id" : user_id 要查询的条件属性-->
        <association property="userInfo"  column="user_id" select="getUserInfoByUserId">

        </association>
    </resultMap>

    <select id="getUserInfoByUserId" parameterType="int" resultType="com.mybatis.demo.model.UserInfo">
        select * from user_info where id=#{id}
    </select>


</mapper>